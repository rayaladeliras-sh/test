package com.stubhub.domain.inventory.v2.listings.intf;
/*

import junit.framework.Assert;

import org.testng.annotations.Test;

import com.stubhub.domain.inventory.v2.listings.intf.ListingAttribute;
import com.stubhub.domain.inventory.v2.listings.intf.ListingResponse;

public class ListingAttributeTest {
	@Test
	 public void testGetSet() {
		
		ListingAttribute listingAttribute=new ListingAttribute();
		Long id=2L;
		String name="NAME";
		
		
		listingAttribute.setId(id);
		Assert.assertEquals(listingAttribute.getId(),id);
		listingAttribute.setName(name);
		Assert.assertEquals(listingAttribute.getName(), name);
		Assert.assertNotNull(new ListingResponse().hashCode());
		Assert.assertTrue(listingAttribute.equals(listingAttribute));
		Assert.assertFalse(listingAttribute.equals(new ListingAttribute()));
		Assert.assertFalse(new ListingAttribute().equals(listingAttribute));
		Assert.assertFalse(new ListingAttribute().equals(null));
		Assert.assertEquals(listingAttribute, listingAttribute);
		Assert.assertNotSame(listingAttribute,new Integer(0));	
		
	
		Object other = new ListingAttribute();
		Object different = new String();
		
		Assert.assertNotNull(listingAttribute.hashCode());
		Assert.assertFalse(listingAttribute.equals(different));
		Assert.assertFalse(listingAttribute.equals(other));
		Assert.assertFalse(listingAttribute.equals(null));
		
		

}

}*/
