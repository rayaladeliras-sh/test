package com.stubhub.domain.inventory.listings.v2.helper;

public class LmsApprovalStatus {
/*
	private Long id;	
	private String description;

	public LmsApprovalStatus() {
	}

	public LmsApprovalStatus(Long value) {
		if (value == null) {
			setId(null);
			setDescription("Not Requested");
		} else if (value.equals(1L)) {
			setId(1L);
			setDescription("Pending Approval");
		} else if (value.equals(2L)) {
			setId(2L);
			setDescription("Approved");
		} else if (value.equals(3L)) {
			setId(3L);
			setDescription("Rejected");
		} else if (value.equals(4L)) {
			setId(4L);
			setDescription("Approval Removed");
		}
	}
	
	public LmsApprovalStatus(String value) {
		if (value == null) {
			setId(null);
			setDescription("Not Requested");
		} else if (value.equalsIgnoreCase("Pending Approval") || value.equalsIgnoreCase("Pending_Approval")) {
			setId(1L);
			setDescription("Pending Approval");
		} else if (value.equalsIgnoreCase("Approved")) {
			setId(2L);
			setDescription("Approved");
		} else if (value.equalsIgnoreCase("Rejected")) {
			setId(3L);
			setDescription("Rejected");
		} else if (value.equalsIgnoreCase("Approval Removed") || value.equalsIgnoreCase("Approval_Removed")) {
			setId(4L);
			setDescription("Approval Removed");
		}
	}

	public void setId(Long id) {
		this.id = id;
	}

	public void setDescription(String description) {
		this.description = description;
	}

	public Long getId() {
		return id;
	}

	public String getDescription() {
		return description;
	}
*/	
}
